name: stock-alerting-checks

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install uv
      run: pip install uv

    - name: Set up cache
      uses: actions/cache@v4
      with:
        path: backend/.venv
        key: venv-${{ runner.os }}-${{ hashFiles('backend/uv.lock') }}

    - name: Install dependencies
      working-directory: backend
      run: uv sync
      if: steps.cache.outputs.cache-hit != 'true'

  check:
    runs-on: ubuntu-latest
    needs: setup
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install uv
      run: pip install uv

    - name: Set up cache
      uses: actions/cache@v4
      with:
        path: backend/.venv
        key: venv-${{ runner.os }}-${{ hashFiles('backend/uv.lock') }}

    - name: Install dependencies
      working-directory: backend
      run: uv sync
      if: steps.cache.outputs.cache-hit != 'true'

    - name: Check code
      working-directory: backend
      run: make check

  tests:
    runs-on: ubuntu-latest
    needs: setup
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install uv
      run: pip install uv

    - name: Set up cache
      uses: actions/cache@v4
      with:
        path: backend/.venv
        key: venv-${{ runner.os }}-${{ hashFiles('backend/uv.lock') }}

    - name: Install dependencies
      working-directory: backend
      run: uv sync
      if: steps.cache.outputs.cache-hit != 'true'

    - name: Run tests
      working-directory: backend
      run: make tests
